const eschaton = {
  fight: (defender, attacker) => {
    // assuming an object as defender and attacker with a strength attribute,
    // depending on who is stronger, defender or attacker wins
    // after that passes, include a shield attribute that helps decide the winner
    //

    //
  }
};

module.exports = eschaton;
