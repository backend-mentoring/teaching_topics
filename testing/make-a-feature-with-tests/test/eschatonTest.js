const { assert, expect } = require('chai');

const eschaton = require('../eschaton');

describe('eschaton', () => {
  it('should exist', () => {
    expect(eschaton).to.exist;
  });

  // write a test to make sure that the stronger beligerent wins

  // then write the code to make it pass

  // then add shields as a thing and make the old test still pass

  // keep making it more complex with your tests guiding you.

  // just get in those reps!


});
