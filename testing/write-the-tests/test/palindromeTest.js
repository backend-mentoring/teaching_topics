const { assert, expect}  = require('chai');

const isPalindrome = require('../isPalindrome');

describe('isPalindrome', () => {
  it('should exist', () => {
    expect(isPalindrome).to.exist;
  });

  // let's think of some cases to test

  // test the happy path and the sad path.

  // if your tests fail on somthing that should have worked, go ahead and alter the code accordingly.



});
